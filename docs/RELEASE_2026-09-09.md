# 2026-09-09 核准整合與發布

使用者原話：「審核通過，commit and push」。批准範圍為本輪四站 UI 與各站 3 單元／9 題／6 練習，不含後續髖、踝足新稿的批准。

受審原稿 `course/research/2026-09-09-advanced/package.json` 保持原位元；SHA-256 `b6826006f1b15b0d49b51f8d6732397091827d363c2cb88e50ab8e0d36fec202`。`approval.json` 保存 UTC 時間、基底、使用者原話、範圍及角色。`course/data/advanced-workshop.json` 為核准副本，僅新增狀態與追溯欄位。原有課程資料與批准不變。

正式 build 產生 `/advanced/`、3 個 `/advanced/units/<id>.html` 靜態講義與互動題目。首頁與課程頁均可進入，教材可返回先備單元。正式顯示只把草稿／待審標籤改為策展審閱通過，移除「未進入正式 build」的過期工作狀態；醫療教學文字未改。

新增 8 個整合防護測試：內容、原稿或收據變動不能承襲批准；單元／題目／病例可撤回，重建刪除舊頁及題目。每站 make check 通過（68 既有 + 14 草稿 + 8 發布 Python 測試）。工作坊正式產物 19 個互動情境通過。

新教材保持 noindex,follow；原課程索引狀態不變。不是臨床能力認證，影片真實串流與 full-video review 不因教材批准而自動完成。

發布目標：已核對 origin/main。Cloudflare build command 為 `python3 src/build/build.py && python3 src/build/audit.py && python3 src/build/audit_medical.py`，純標準庫，相同命令已涵蓋於本機 check。部署結果以後續平台 commit 與線上 SHA 核對為準。
